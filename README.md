# Homepage

## Local development

Use a project-local Bundler install path so Ruby gems are not written to system directories:

```bash
bundle install
bundle exec jekyll serve
```

Build once without serving:

```bash
bundle exec jekyll build
```
