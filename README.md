# Homepage

Personal site — a static [Jekyll](https://jekyllrb.com/) site that builds on GitHub Pages

---

## Run it locally

```bash
bundle install
bundle exec jekyll serve
```

Open <http://localhost:4000>. Pages rebuild as you save.
